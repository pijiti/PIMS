
        td  = b.input :brand_id, collection: Brand.all, value_method: :id, label_method: :detailed_info, label: false, prompt: "Select Brand"
        td  = b.input :rate, label: false, placeholder: "Rate", as: :string, maxlength: 5
        td  = b.input :qty, label: false, placeholder: "Quantity", as: :string, maxlength: 5
