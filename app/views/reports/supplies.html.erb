<div class="row">
  <h3 class="text-center">Supply Reports</h3>
  <div class="col-xs-12">
    <%= select_tag("marketer", options_for_select(Marketer.all.pluck(:name, :id)) ,  include_blank: true , class: "form-control input-transparent report-filter") %>
    <%= select_month(Date.today, {}, {class: 'form-control input-transparent report-filter', id: 'filter_month'}) %>
    <%= select_year(Date.today, {}, {class: 'form-control input-transparent report-filter', id: 'filter_year'}) %>

    <div id="supplies_table">
      <%= render 'requisition_table' %>
    </div>

  </div>
</div>



<script type="text/javascript">
  $(".report-filter").on("change", function () {
    $.ajax({
      url: '/reports/supplies_filter_by_month',
      type: 'post',
      data: { marketer: $('#marketer').val(), month: $("#filter_month").val() , year:$("#filter_year").val() }
    });
  });
</script>